#include "main.h"
#include <unistd.h>

char *intstr(int);

int main(void)
{
	char *str = intstr(0);
	char a = 10;
	
	write(1, str, 1);
//	write(1, &a, 1);
}

char *intstr(int a)
{
	char *b, *c;
	int i, d, len;

	if (a == 0)
	{
		i = 0;
		b[i++] = '0';
//		b[i] = 0;
	}
/*	else if (a < 0)
	{
		*b = '-';
		a = a * (-1);
		i = 0;
		while (a != 0)
		{
			d = a % 10;
			a = a / 10;
			c[i++] = d + '0';
			c[i] = 0;
		}
		len = _strlen(c);
		i = 1;
		while (len > 0)
		{
			b[i++] = c[len-- - 1];
		}
		b[i] = 0;
	}
	else
	{
		i = 0;
		while (a != 0)
		{
			d = a % 10;
			a = a / 10;
			c[i++] = d + '0';
			c[i] = 0;
		}
		len = _strlen(c);
		i = 1;
		while (len > 0)
		{
			b[i++] = c[len-- - 1];
		}
		b[i] = 0;
	}*/
	return (b);
}
