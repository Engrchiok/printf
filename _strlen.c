/**
 * _strlen - function that computes length of a string.
 * @string: function parameter.
 * Return: always len.
 */

int _strlen(const char *string)
{
	int a, len;

	for (a = 0, len = 0; string[a] != 0; a++, len++)
	{
	}
	return (len);
}
